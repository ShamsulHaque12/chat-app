import 'package:flutter/material.dart';
class ReadChat extends StatelessWidget {
  const ReadChat({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        crossAxisAlignment: CrossAxisAlignment.start,
        children: [
          Text("Read Chat"),
        ],
      ),
    );
  }
}
